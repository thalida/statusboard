name: Weekly Trivy Scan

on:
  schedule:
    # Mondays at 06:00 UTC.
    - cron: '0 6 * * 1'
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: read
      issues: write # to open an issue on new findings
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Run Trivy on :latest
        id: trivy
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ghcr.io/${{ github.repository_owner }}/statusboard-api:latest
          format: json
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: 0 # do not fail; open an issue instead
          output: trivy-results.json

      - name: Check for findings
        id: findings
        # Parse Trivy's JSON rather than grepping its table, which is a
        # rendering that upstream is free to change.
        run: |
          count=$(jq '[.Results[]?.Vulnerabilities // [] | length] | add // 0' trivy-results.json)
          echo "count=$count" >> "$GITHUB_OUTPUT"
          if [ "$count" -gt 0 ]; then
            echo "found=true" >> "$GITHUB_OUTPUT"
          else
            echo "found=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Open issue on findings
        if: steps.findings.outputs.found == 'true'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const json = JSON.parse(fs.readFileSync('trivy-results.json', 'utf8'));
            const vulns = (json.Results || []).flatMap(r => r.Vulnerabilities || []);
            const summary = vulns
              .map(v => `- **${v.Severity}** ${v.VulnerabilityID} in ${v.PkgName}@${v.InstalledVersion}: ${v.Title}`)
              .join('\n');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `Trivy scan: ${vulns.length} HIGH/CRITICAL CVEs in statusboard-api:latest (${new Date().toISOString().split('T')[0]})`,
              body: `Weekly scan found ${vulns.length} HIGH/CRITICAL CVEs in the published image. Time to rebuild against a fresh base.\n\n${summary}`,
              labels: ['security', 'dependencies'],
            });
